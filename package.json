{
  "name": "backend-roi-calculator",
  "version": "0.1.0",
  "private": true,
  "description": "Node.js backend placeholders for ROI PDF generation and grants lookup",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test:report": "node scripts/test-report.js --mock",
    "test:report:live": "node scripts/test-report.js",
    "test:report:preview": "node scripts/test-report.js --preview --mock"
  },
  "engines": {
    "node": ">=18"
  }
}
